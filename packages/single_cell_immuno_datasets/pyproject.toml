[project]
name = "single_cell_immuno_datasets"
version = "0.1.0"
dependencies = [
    "returns",
    "polars",
    "altair",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
